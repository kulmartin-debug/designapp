{
  "name": "pred-po-vizualizator",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && node scripts/copy-frontend-dist.mjs && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "migrate:deploy": "npm run --prefix backend prisma:migrate:deploy"
  }
}
